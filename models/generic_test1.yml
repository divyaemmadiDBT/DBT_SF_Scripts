models:
  - name: stg_test_ex1
    columns:
      - name: id
        tests: 
        - unique
        - not_null
        - relationships:
           arguments:
            to: ref('orders_test_relationship')
            field: customer_id
      - name: shipped_status
        tests:
        - accepted_values:
            arguments: 
             values: ['Shipped','Returned']

