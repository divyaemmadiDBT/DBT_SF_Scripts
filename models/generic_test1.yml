models:
  - name: stg_test_ex1
    columns:
      - name: id
        tests: 
        - unique
        - not_null
      - name: shipped_status
        tests:
        - accepted_values:
            arguments: 
             values: ['Shipped','Returned']
         - name: customer_id
        data_tests:
          - relationships:
              arguments:
                to: ref('stg_test_ex1')
                field: id
      
