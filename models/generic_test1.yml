models:
  - name: stg_test_ex1
    columns:
      - name: id
        tests: 
        - unique
        - not_null
      - name: shipped_status
        tests:

        - accepted_values:
            arguments: 
             values: ['Shipped','Returned']
        
      
